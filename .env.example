# This site is fully static (frontend-only) — no backend, no database.
# No environment variables are required to build or deploy on Vercel.
#
# Optional: set your production domain so /sitemap.xml and /robots.txt
# emit absolute URLs. Defaults to https://fashionasialtd.com if unset.
# NEXT_PUBLIC_SITE_URL="https://your-domain.com"
